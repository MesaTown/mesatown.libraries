function main() {
    return {
        meta: {
            help: 'Returns the current state to default\n(can also use `home` or `town`)',
        },
        require: [],
        execute: () => {},
    }
}