function main(){
    return {
        descript: "Returns the current state to default\n(can also use [home])",
        marker: null,
        aliases: "home",
        require: [],
        execute: () => {},
    }
}